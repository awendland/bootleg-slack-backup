{
  "name": "bootleg-slack-backup",
  "version": "0.1.0",
  "description": "Headless browser based backup of Slack channels (free or paid)",
  "main": "index.js",
  "author": "Alex Wendland <me@alexwendland.com>",
  "license": "MIT",
  "scripts": {
    "start": "yarn build && bin/bsa",
    "build": "tsc --build"
  },
  "dependencies": {
    "puppeteer": "^3.0.2"
  },
  "devDependencies": {
    "@types/puppeteer": "^2.0.1",
    "typescript": "^3.8.3"
  }
}
